class Cell:
  
    def __init__(self, x_coord: str, y_coord: str):
        self.x_coord = x_coord
        self.y_coord = f"SOME RANDOM TEXT: {y_coord}"

    def set_value (self, value, x_coord, y_coord)
      self.value(x_coord,y_coord) = value
      return value

    def get_value (self):
      return self.value

    def coord_value(self, x_coord, y_coord):
      coord_value=self.(x_coord,y_coord).value
      return coord_value

      
        
        
